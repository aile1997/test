// import { Demo } from 'runafe-threejs';
import { JsxDemo as CountDemo } from 'runafe-threejs';

export default {
  title: 'Intro/Playground',
};

export const Main = () => (
  <div>
    <CountDemo count={6} />
  </div>
);
